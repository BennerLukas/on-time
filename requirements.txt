tensorflow  # if you want to use a gpu -> install CUDA manually - for GPU support conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0 (https://www.tensorflow.org/install/pip#windows_1)
ray
# gym # will be installed automatically with ray
numpy